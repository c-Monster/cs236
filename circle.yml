machine:
    services:
        - docker

dependencies:
    pre:
        - sudo apt-get update
    override:
        - sudo apt-get install valgrind

compile:
    pre:
        - cd source

test:
    override:
        - cd source 
        - valgrind --leak-check=yes a.out in40.txt

