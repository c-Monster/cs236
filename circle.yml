machine:
    services:
        - docker

dependencies:
    pre:
        - sudo apt-get update
    override:
        - sudo apt-get install valgrind

compile:
    pre:
        - cd source

test:
    override:
        - valgrind *.h *.cpp

